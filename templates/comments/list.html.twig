{% for comment in comments %}
    <div class="card m-2">
        <div class="card-body">
            <h5 class="card-title">Comment</h5>
            <p class="card-text">{{ comment.content }}</p>
        </div>
    </div>
{% endfor %}